import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lab9',
  appName: 'lab9',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
